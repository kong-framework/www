window.SIDEBAR_ITEMS = {"constant":[["HOSTNAME_LENGTH_LIMIT","The issuer of the `kpassport` can be a, the maximum length 45bytes because that is the maximum IPv6 string length.  But any string identifier can be used not just IP addresses as long as it fits into 45bytes"],["TIMESTAMP_LENGTH","The length of a timestamp created by chrono::Utc::now();"],["USERNAME_LENGTH_LIMIT","The length of a username A username cannot be longer than 15 characters. A username can only contain alphanumeric characters (letters A-Z, numbers 0-9) with the exception of underscores, as noted above."]]};